import 'package:flutter/material.dart';

class LiveTarotScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Canlı Tarot')),
      body: Center(child: Text('Canlı Tarot Falı Ekranı')),
    );
  }
}
